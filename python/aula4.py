numero = int(input("Digite um numero:"))

if numero % 2 == 0:
    print("Numero par")
else:
    print("numero impar")

# Pede para o usuário digitar um número e converte para inteiro (int),
# porque o input() sempre retorna texto (string).

#numero = int(input("Digite um numero:"))

# Aqui começa a estrutura condicional.
# O operador % (módulo) pega o resto da divisão.
# Se o resto da divisão do número por 2 for igual a 0, significa que ele é par.

#if numero % 2 == 0:

    # Se a condição acima for verdadeira, executa esta linha:

   # print("Numero par")

#else:

    # Caso contrário (resto diferente de 0), executa esta linha:

  #  print("numero impar")
